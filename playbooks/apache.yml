---
- hosts: all
  tasks:
    
    - yum:
        name: httpd
        state: present
      when: ansible_os_family == 'RedHat'

    - apt:
        name: apache2
        state: present
      when: ansible_os_family == "Debian"
...
